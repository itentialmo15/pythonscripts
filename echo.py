#!/usr/bin/env python3
import sys
import argparse
if __name__ == "__main__":
    if len(sys.argv) < 2:
        print("Usage: python echo.py <text>")
        sys.exit(1)

    text = parser.add_argument('--command', required=True, help="Command to run")
    print(text)

